{{block filmTitle(title, class, element = "h1", forceText = false)}}

  {{titleImage := .ImageMap["Title"]}}

  {{if isset(titleImage) && !forceText}}
    <img src="{{titleImage}}" alt="{{title}}" class="item-title-image {{class}}" />
  {{else}}
    <{{element}}>{{title}}</{{element}}>
  {{end}}
{{end}}
